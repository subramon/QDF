\section{Instructions to LLM}

Generate and return the following 
\be
\item optimized C code the the function \cfunc\ described above as
  \be
\item a {\tt .c} file named {\cfunc}.c
\item a {\tt .h} file named {\cfunc}.h
  containing the function declaration
  \ee
\item the command to compile {\cfunc}.c, link necessary libraries nd create a
  {\tt .so} file named lib{\cfunc}.so
  \ee

\end{document}
